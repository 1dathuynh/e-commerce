mixin search(keyword)
	form(id='form-search')
		.input-group
			input(type='text', name='keyword', placeholder='Tìm kiếm', class='form-control', value=keyword)
			.input-group-append
				button(type='submit', class='btn btn-success') Tìm kiếm